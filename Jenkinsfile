pipeline {
    agent any
        stage('Build') {
            steps {
                echo "Building HTML project..."
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploying project..."
            }
        }
    }
}
